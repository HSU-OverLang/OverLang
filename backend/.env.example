# Database 설정 (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=overlang
DB_USERNAME=your_username
DB_PASSWORD=your_password

# Firebase 설정 (Admin SDK)
# 파일은 backend/src/main/resources/ 경로에 위치
FIREBASE_SERVICE_ACCOUNT_PATH=your-firebase-admin.json
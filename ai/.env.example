CUDA_VISIBLE_DEVICES=0

# Redis (Local: localhost, Docker: redis)
# Docker 실행 시: docker-compose --profile gpu-only up -d
REDIS_HOST=localhost
REDIS_PORT=6379

# Database (Local: localhost, Docker: postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=overlang
DB_USERNAME=your_username
DB_PASSWORD=your_password